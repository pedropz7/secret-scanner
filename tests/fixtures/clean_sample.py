# Arquivo limpo: escanear isso não deve gerar nenhum alerta.


def add(a: int, b: int) -> int:
    return a + b


GREETING = "hello world"
COMMENT = "isto é só um comentário comum, sem nenhum segredo"
